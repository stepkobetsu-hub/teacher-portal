# teacher-portal top button v2
出勤確認の入力画面・送信完了画面にも「←トップへ」を追加。入退くんQR画面の左上ロゴ/タイトルを「←トップへ」に変更。


# teacher-portal staff link
管理者QR登録をトップから削除し、最下部にスタッフ用アプリへのリンクを追加。

## 業務ホームからの直接起動

- 授業報告: `/?view=attendance`
- 出退くんQR: `/?view=qr`

`view` を指定しない既存URLは、従来どおり講師ポータルのトップを表示する。「トップへ」で直接起動指定を外す。
